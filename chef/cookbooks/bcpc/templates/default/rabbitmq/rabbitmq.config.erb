%##############################################################################
%                          generated by chef
%##############################################################################

[
  {
    kernel,
    [
      {
        inet_dist_use_interface, { <%= node['ipaddress'].gsub('.',',') %> }
      }
    ]
  },
  {
    rabbitmq_management,
    [
      { listener, [{port, 55672}, {ip, "<%= node['ipaddress'] %>"}] },
      { redirect_old_port, false }
    ]
  },
  {
    rabbit,
    [
      {loopback_users, []},
      {heartbeat, <%= node['bcpc']['rabbitmq']['heartbeat'] %>},
      {cluster_partition_handling, pause_minority},
      {tcp_listeners, [{"<%= node['ipaddress'] %>", 5672}]}
    ]
  }
].

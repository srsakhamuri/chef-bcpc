name: bootstrap
description: bootstrap node
json_class: Chef::Role
chef_type: role
run_list:
  - "role[node]"
  - "recipe[bcpc::ssh]"
  - "recipe[bcpc::file-server]"
  - "recipe[bcpc::ufw]"
  - "recipe[bcpc::rally]"
